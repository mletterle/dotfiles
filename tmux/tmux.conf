# Change prefix to Ctrl-u
unbind C-u
set -g prefix C-u
bind -n C-u send-prefix

# Use Vi bindings
set -g mode-keys vi
set -g status-keys vi

# Status bar settings
set -g status on
set -g status-left-length 60
set -g status-right-length 50


set -g status-style "fg=color246,bg=color240"

set -g status-left "\
#[fg=color249]  #h #[fg=color240, bg=color246] \
 #(whoami) #[fg=color246, bg=default]\
#[fg=color249]  #S #[fg=color240, bg=color246] \
#[fg=color246, bg=default]"

set -g window-status-format "  #W#F"
set -g window-status-current-format "  #W#F"

set -g status-right "\
#[fg=color240,bg=color246]%b%e %R "

# Easy reload conf file
bind r {
  source-file ~/.tmux.conf
  display 'Config reloaded! 🚀'
}


# Split panes using |, \, and -
bind \\ split-window -h
bind | split-window -h
bind - split-window -v
unbind '"'
unbind %

# Close window using Prefix+q
bind q confirm-before -p "Kill this window?" kill-window

# Vim-like switching panes
bind -n M-h select-pane -L
bind -n M-j select-pane -D
bind -n M-k select-pane -U
bind -n M-l select-pane -R

# Shift arrow to switch windows
bind -n M-Left  previous-window
bind -n M-Right next-window

# Don't rename windows automatically
set-option -g allow-rename off

# Slide windows into empty slots
set -g renumber-windows on

# Smart pane switching with awareness of Vim splits.
# See: https://github.com/christoomey/vim-tmux-navigator
vim_pattern='(\S+/)?g?\.?(view|l?n?vim?x?|fzf)(diff)?(-wrapped)?'
is_vim="ps -o state= -o comm= -t '#{pane_tty}' \
    | grep -iqE '^[^TXZ ]+ +${vim_pattern}$'"
bind-key -n 'C-h' if-shell "$is_vim" 'send-keys C-h'  'select-pane -L'
bind-key -n 'C-j' if-shell "$is_vim" 'send-keys C-j'  'select-pane -D'
bind-key -n 'C-k' if-shell "$is_vim" 'send-keys C-k'  'select-pane -U'
bind-key -n 'C-l' if-shell "$is_vim" 'send-keys C-l'  'select-pane -R'
tmux_version='$(tmux -V | sed -En "s/^tmux ([0-9]+(.[0-9]+)?).*/\1/p")'
if-shell -b '[ "$(echo "$tmux_version < 3.0" | bc)" = 1 ]' \
    "bind-key -n 'C-\\' if-shell \"$is_vim\" 'send-keys C-\\'  'select-pane -l'"
if-shell -b '[ "$(echo "$tmux_version >= 3.0" | bc)" = 1 ]' \
    "bind-key -n 'C-\\' if-shell \"$is_vim\" 'send-keys C-\\\\'  'select-pane -l'"

bind-key -T copy-mode-vi 'C-h' select-pane -L
bind-key -T copy-mode-vi 'C-j' select-pane -D
bind-key -T copy-mode-vi 'C-k' select-pane -U
bind-key -T copy-mode-vi 'C-l' select-pane -R
bind-key -T copy-mode-vi 'C-\' select-pane -l
